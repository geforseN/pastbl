fix:
  - make tests work
    - install playwright
    - remove dead tests
    - remove .skip and .todo

feat:
  - add autocomplete in pasta-form textarea (suggest emotes tokens)
  - ! add refresh for emotes-integrations (global and persons):
      - add refresh for persons emotes collections
  - use WebShare API for published pasta (https://www.youtube.com/watch?v=lt77LscZcn0)
  - add chrome & firefox extensions
  - add TEmoteIntegrations and TPersonEmoteCollection auto-imports (see emote-integrations/add-type-namespaces-autoimport branch)
  - return vueMacros eslint config back ('globals' key is not allowed flat in config)

docs:
  - refactor README

perf:
  - optimize dev & build scripts
    - icon for carbon, ic + solar (hamburger icon) + material symbols
    - tailwindcss
    - nuxt/ui

?: rename integrations to providers
