describe.todo("chat-pasta-tag-add-dialog", () => {
  test.todo("call will correct data and modal is opened");
  test.todo("call will incorrect data and modal is opened");
  test.todo(
    "call again with any data and modal is opened (must ensure that after incorrect data (and thrown error) modal will be opened again)",
  );
});
